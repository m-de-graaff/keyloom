import type { KeyloomAdapter } from '@keyloom/core/adapter-types'

export default function postgresAdapter(..._args: any[]): KeyloomAdapter {
  throw new Error('PostgreSQL adapter not implemented yet')
}
