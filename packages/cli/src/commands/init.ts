export async function initCommand(_args: string[]) {
  console.log('keyloom init — not yet implemented in this branch')
  console.log(
    'Scaffold planned: keyloom.config.ts, handler, middleware, env, adapters, templates, deps',
  )
}
