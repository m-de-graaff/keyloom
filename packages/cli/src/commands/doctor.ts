export async function doctorCommand(_args: string[]) {
  console.log('keyloom doctor  not yet implemented in this branch')
  console.log('Planned checks: env/secrets, cookie policy, baseUrl reachability, DB connectivity, clock skew, manifest wiring, provider redirects')
}

